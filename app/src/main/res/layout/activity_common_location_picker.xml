<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#1F1B33">

    <!-- MAP VIEW: Takes up all space ABOVE the bottom panel -->
    <org.osmdroid.views.MapView
        android:id="@+id/map"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toTopOf="@+id/bottomContainer"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!--
      CENTER PIN
      Constrained to the map boundaries, NOT the parent.
      translationY shifts it up exactly half its height so the bottom tip is the true center.
    -->
    <ImageView
        android:id="@+id/centerPin"
        android:layout_width="46dp"
        android:layout_height="46dp"
        android:src="@drawable/ic_pin"
        app:tint="#D50000"
        android:translationY="-23dp"
        app:layout_constraintTop_toTopOf="@id/map"
        app:layout_constraintBottom_toBottomOf="@id/map"
        app:layout_constraintStart_toStartOf="@id/map"
        app:layout_constraintEnd_toEndOf="@id/map" />

    <!-- BOTTOM PANEL: Gives the map a clean boundary -->
    <LinearLayout
        android:id="@+id/bottomContainer"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingHorizontal="24dp"
        android:paddingTop="24dp"
        android:paddingBottom="40dp"
        android:background="@drawable/bg_container"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <!-- PREMIUM BLACK BUTTON (35dp Radius) -->
        <android.widget.Button
            android:id="@+id/btnConfirmLocation"
            android:layout_width="match_parent"
            android:layout_height="60dp"
            android:text="Set This Location"
            android:textAllCaps="false"
            android:textColor="#FFFFFF"
            android:textSize="16sp"
            android:textStyle="bold"
            android:background="@drawable/bg_btn_black" />

    </LinearLayout>

</androidx.constraintlayout.widget.ConstraintLayout>